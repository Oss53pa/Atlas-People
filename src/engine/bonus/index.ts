/**
 * Moteur Bonus (M3, variable) — point d'entrée.
 * Calcul déterministe en Money.ts bigint (R5). Consomme le score validé (§9) ;
 * ne recalcule jamais le score ni n'autorise PROPH3T à calculer un bonus.
 */
export * from './types';
export * from './compute';
