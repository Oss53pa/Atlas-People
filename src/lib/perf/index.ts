/**
 * Noyau Performance (M7/M8) — façade lib.
 * Réexporte le moteur de calcul déterministe (engine) et la couche live RPC.
 */
export * from '../../engine/performance';
export * from './supabaseLive';
