/**
 * Atlas People — Schémas TypeScript des documents persistés via dataProvider.
 *
 * Source de vérité pour les documents que les composants UI manipulent.
 * Le DataProvider (Supabase + Demo) traduit ces types vers/depuis atlas_people.*.
 */
export type { ReportDoc, ReportTemplate } from '../engine/reportBlocks';
