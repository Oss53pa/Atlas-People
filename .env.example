# Atlas People — infrastructure de confiance Atlas Studio
# Laisser vide pour fonctionner en mode démo (données mockées, aucun appel réseau).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Secrets de l'Edge Function `post-to-fna` (déversement comptable Atlas FNA).
# À définir CÔTÉ SERVEUR uniquement (jamais exposés au front — souveraineté §2.5) :
#   supabase secrets set ATLAS_FNA_API_URL=... ATLAS_FNA_API_KEY=...
